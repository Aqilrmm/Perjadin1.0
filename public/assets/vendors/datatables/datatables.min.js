// Minimal DataTables stub: calls to $(...).DataTable() return a simple API
(function(){
  if (!window.jQuery) return;
  var $ = window.jQuery;
  // if the shim above already implements DataTable, skip
  // provide a no-op plugin if needed
  // Nothing else required as the jQuery shim provides a DataTable method.
})();

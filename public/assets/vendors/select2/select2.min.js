(function(){
  if (!window.jQuery) return;
  var $ = window.jQuery;
  // the jQuery shim already implemented a basic select2 on the wrapper
  // provide a compatibility function on window in case code expects it
  window.Select2 = window.Select2 || {};
})();
